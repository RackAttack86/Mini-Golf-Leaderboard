# Production Environment Configuration
# Copy this file to .env.production and fill in your actual values

# Flask Environment (keep as production)
FLASK_ENV=production

# Flask Secret Key
# Generate a secure random key using: python -c "import secrets; print(secrets.token_hex(32))"
# CRITICAL: Use a strong, unique key for production!
SECRET_KEY=CHANGE-THIS-TO-A-SECURE-RANDOM-KEY

# Google OAuth Configuration
# Get these from: https://console.cloud.google.com/
# 1. Create a project (or use existing)
# 2. Enable Google+ API
# 3. Create OAuth 2.0 credentials
# 4. Add authorized redirect URIs (see below)
GOOGLE_OAUTH_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_OAUTH_CLIENT_SECRET=your-client-secret

# Authorized Redirect URIs to add in Google Cloud Console:
# For production with HTTPS: https://your-domain.com/login/google/authorized
# For production without HTTPS (local network): http://your-nas-ip:8080/login/google/authorized
# For local testing: http://localhost:5001/login/google/authorized
